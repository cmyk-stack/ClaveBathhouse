{
  "buildCommand": "npm run build --workspace @gravity/client",
  "installCommand": "npm install",
  "outputDirectory": "packages/client/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
